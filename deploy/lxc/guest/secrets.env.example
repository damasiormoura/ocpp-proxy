# Install to /etc/ocpp-proxy/secrets.env — mode 600, owner root:ocpp.
# Never commit the real file. Environment variables override YAML per-parameter.
OCPP_PROXY_MQTT__USERNAME=ocpp_proxy
OCPP_PROXY_MQTT__PASSWORD=replace-me
